@{
    ViewBag.Title="陪伴空间";
}

<div class="container peiban-container">

    <div class="row">
        <div class="col-md-6">
            <h3 style="margin: 20px 0px 40px 0px;">登陆</h3>
            <div class="row">

                @if (!string.IsNullOrEmpty(ViewBag.msg))
                { 
                     <div class="alert alert-warning alert-dismissible" role="alert">
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <strong>Warning!</strong> @Html.Raw(ViewBag.msg)
                </div>
                
                }
                
                @using (Html.BeginForm("Login", "RenAccount", FormMethod.Post, new { @class = "form-horizontal", @name = "LoginForm", @id = "LoginForm" }))
                {
                    <div class="form-group">
                       
                        <label class="control-label col-sm-2">手机账号</label>
                        <div class="col-sm-6">
                            <input class="form-control" name="username" id="username" type="text" placeholder="请输入手机号" />
                        </div>
                    </div>

                    <div class="form-group">
                       
                        <label class="control-label col-sm-2">密码</label>
                        <div class="col-sm-6">
                            <input class="form-control" name="password" id="password" type="password" placeholder="请输入密码" />
                        </div>
                    </div>

                    <div class="form-group">
                        <div class="col-sm-6 col-sm-offset-2">
                            @*<span class="pull-left"><a href="###">忘记密码？</a></span>*@
                            <button class="btn btn-success pull-right" type="submit">&nbsp;&nbsp;&nbsp;登陆&nbsp;&nbsp;&nbsp;</button>
                        </div>
                    </div>

                }

            </div>
           
        </div>
        <div class="col-md-6" style="margin-top: 80px;line-height:30px;">
            没有账号？扫描下方二维码注册账号<br /><br />
            <img src="/Resource/img/qrcode_peiban.jpg" class="image" />
            <br /><br />
            1、<strong>注册方式：</strong>点击个人中心=》注册账号 免费注册<br />
            2、<strong>忘记密码：</strong>请联系微信小助手：peibanshichengzhagn
            
        </div>
    </div>

</div>


@section BottomScripts {
    <script type="text/javascript">
       
    </script>
}
